# Keep ALL pnpm workspace package roots present in the build context
# (artifacts/*, lib/*, scripts). Omitting a workspace member breaks
# `pnpm install --frozen-lockfile` inside Docker.

node_modules
**/node_modules
.data
.git
.env
.env.*
!.env.example
**/dist
coverage
.expo
**/.expo
docs
*.md
!README.md
Umumiy rasmlar
attached_assets
.replit
.replitignore
agent-transcripts
